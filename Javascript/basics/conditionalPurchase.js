var isLoggedIn = true;
var isEmailVerified = false;
var cardInfo = true;
if(isLoggedIn && isEmailVerified && cardInfo){
    console.log("Allow User to make a Purchase!");
}else{
    console.log("You are not alllowed to make any purchase!");
}